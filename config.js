// ⚙️ Site config — fill these in to enable SHARED ratings + on-site submissions via Supabase.
// Leave blank to keep the static fallback (on-device ratings + pre-filled GitHub-issue submit).
// The anon key is SAFE to commit publicly — it is protected by Row-Level Security.
// Setup: create a free Supabase project → run supabase/schema.sql in its SQL editor →
//        paste the Project URL + anon public key below → commit.
window.SUPABASE_URL = "https://gsgysqhbktpugpulahqy.supabase.co";
window.SUPABASE_ANON_KEY = "";  // paste your project's anon / publishable key here
